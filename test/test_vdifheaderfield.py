import pytest
from vdifheader import VDIFHeaderField as Field
pytestmark = pytest.mark.fast

# test total bit counts
# test no overlap with header positions
# test encoders/decoders
# test binary trim on set

